import wait from "@/utils/wait";


const PopularProducts = async() => {
await wait()
    return (
        <div className=" mt-10">
           <h1>This is popular product section</h1> 

        </div>
    );
};

export default PopularProducts;